{% extends 'base.html.twig' %}

{% block title %}Hello TrickController!{% endblock %}


{% block body %}


    <h1>bienvenue dans ce blog</h1>

    <p>Lorem ipsum dolor sit amet, consectetur adipisicing elit. A architecto commodi cupiditate esse est facere fugiat
        illo
        incidunt inventore libero modi molestias mollitia neque nisi numquam placeat quas qui quibusdam quod ratione
        reiciendis repellat reprehenderit, saepe totam unde voluptate voluptatibus!</p><p>Animi atque, beatae deleniti
    id
    inventore magni minus molestiae nemo nihil provident quae qui quibusdam ratione reiciendis sed! Culpa deserunt eos
    et nihil placeat quaerat, reiciendis? Architecto consequuntur eaque eius fugit natus non officia praesentium
    tempore. A ad modi temporibus!</p>



    <div class="container">
        <div class="row flex">
            {% for trick in tricks %}
                <div class="col-3">
                    <div class="card">
                        <img src="{#{{ trick.images }}#}" alt="" class="card-img-top">
                        <div class="card-body">
                            <h5 class="card-title">{{ trick.name | slice(0, 20) }}</h5>
                            <h2 class="card-subtitle mb-2 text-muted">
                                {{ trick.createdAt | date('d/m/Y') }}
                            </h2>
                            <p class="card-text">
                                {#{{ trick.description | raw | slice(0, 50) ~ '...' }}#}
                                <a href="{{ path('trick_show', {'id': trick.id}) }}" class="btn btn-primary">Lire la
                                    suite</a>
                                <a href="{{ path('trick_edit', {'id': trick.id}) }}" class="btn btn-primary">Editer
                                    l'article</a>
                            </p>
                        </div>
                    </div>
                </div>
            {% endfor %}
        </div>
    </div>

{% endblock %}
