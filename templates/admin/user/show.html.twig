{% extends 'base.html.twig' %}

{% block title %}Utilisateur{% endblock %}

{% block body %}
    <div class="container">
        <h1>Utilisateur {{ user.username }}</h1>

        <table class="table">
            <tbody>
            <tr>
                <th>Id</th>
                <td>{{ user.id }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ user.email }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ user.username }}</td>
            </tr>
            {#<tr>
                <th>Mot de passe</th>
                <td>{{ user.password }}</td>
            </tr>#}
            <tr>
                <th>Photo</th>
                <td>{{ user.photo }}</td>
            </tr>
            {#<tr>
                <th>ActivationToken</th>
                <td>{{ user.activationToken }}</td>
            </tr>
            <tr>
                <th>ResetToken</th>
                <td>{{ user.resetToken }}</td>
            </tr>#}
            </tbody>
        </table>

        <a href="{{ path('user_index') }}" class="btn btn-info">Revenir</a>

        {{ include('admin/user/_delete_form.html.twig') }}
    </div>
{% endblock %}
